(lambda ()
  (let ((children (list (list "a" 'A) (list (list "b" 'B)))))
    (let ((routing (list
                    (list (list "a" "each link") (list "b" "many links"))
                    )))
      ...
      )))
